TEAM_ID=8758U5C85V

PRODUCT_NAME=DocScanner
PRODUCT_BUNDLE_IDENTIFIER=org.bibletranslationtools.docscanner$(TEAM_ID)

CURRENT_PROJECT_VERSION=12
MARKETING_VERSION=0.5.2

// Shared KMP framework only targets iosSimulatorArm64 (no x86_64 sim slice),
// so exclude x86_64 for the simulator -> "Any iOS Simulator Device (arm64)".
EXCLUDED_ARCHS[sdk=iphonesimulator*]=x86_64
